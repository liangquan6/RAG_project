# 核心依赖（在 Python 3.10+ 下测试，建议使用 conda 环境 RAG_env）
# 安装：pip install -r requirements.txt

# 配置与环境
python-dotenv==1.2.2
PyYAML==6.0.3

# LangChain / Agent
langchain==1.3.1
langchain-core==1.4.0
langchain-community==0.4.2
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
langgraph==1.2.1

# 模型与向量库
dashscope==1.25.19
chromadb==1.5.9

# Web 前端
streamlit==1.57.0

# 文档解析
pymupdf==1.27.2.3

# HTTP（天气 / 定位 API）
httpx==0.28.1

# RAG 评估（可选，跑 eval 时需要）
ragas==0.4.3
datasets==4.8.5
pandas==2.3.3
